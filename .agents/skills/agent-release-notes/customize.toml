# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for agent-release-notes.
# Team overrides:     {project-root}/_bmad/custom/agent-release-notes.toml
# Personal overrides: {project-root}/_bmad/custom/agent-release-notes.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "release-notes"
name = "Release Notes"
title = "Release Notes Agent"
icon = "📋"
description = "Fetches TFS/Azure DevOps work items and drafts release notes from them."
agent_type = "memory"   # stateless | memory | autonomous
